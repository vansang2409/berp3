# README
########

This directory contains:

data:      A directory with SQL script to add Berp3 initial data.
migration: A directory with SQL script to migrate Berp3 database from 
           one version to another version.
tables:    A directory with all files with create tables and index orders.
